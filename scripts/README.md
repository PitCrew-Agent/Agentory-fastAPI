# scripts

일회성 실행 스크립트 모음.

| 스크립트 | 용도 |
| --- | --- |
| `seed_data.py` (예정) | equipment_master·telemetry 샘플 데이터 적재 (DEV_DATABASE) |
| `ingest_manuals.py` (예정) | 매뉴얼 파싱→청킹→임베딩→적재 실행 (AI_RAG01) |

실행 방법: `uv run python scripts/<파일명>.py`
